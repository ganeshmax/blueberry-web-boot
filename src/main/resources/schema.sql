# Add DDL statements here once you decide to get away from ddl-auto. Maybe use liquibase or autofly instead.
# create table users (
#   id integer not null auto_increment,
#   email varchar(100) not null,
#   pwd varchar(100) not null,
#   phone varchar(100),
#
#   primary key (id)
# );
